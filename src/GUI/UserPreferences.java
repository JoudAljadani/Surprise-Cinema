package GUI;
import java.util.HashSet;
import java.util.Set;

public class UserPreferences {
    //Stores the genres selected by the current user
    public static Set<String> selectedGenres = new HashSet<>();
}